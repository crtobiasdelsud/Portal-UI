// === Adapters ===
export {
  AdaptersProvider,
  useAdapters,
  useOptionalAdapters,
} from './adapters/AdaptersContext.jsx'

// === Data layer ===
export {
  ArticlePoolProvider,
  useArticlePool,
  createArticlePool,
  useArticles,
} from './data/index.js'

// === Components ===
// (se irán agregando en cada fase de migración)
